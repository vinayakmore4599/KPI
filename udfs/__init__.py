"""UDF package copied into the host as `udfs/`.

What this file provides
    The deployable tree: sotif/main.py (entry), kpi_engine/, config/.

Where it is used
    Metadata module_path: udfs.sotif.main

When to use
    Copy this whole `udfs/` folder into the platform. Do not copy tests/ or
    docs; they stay in the repo for development.
"""
